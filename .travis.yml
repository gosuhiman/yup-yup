dist: trusty
sudo: required
addons:
  chrome: stable

before_install:
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9876 http://localhost &

script:
  - cd frontend
  - npm run travis:test